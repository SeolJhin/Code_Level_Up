기본 질문
forEach, map, filter 차이점
event loop 동작 원리
spread 문법, rest 문법
restFull -> patch, put 차이점
ioc, di 설명
의존성 주입 방법 (3가지)
class, interface 차이
Annotation 동작원리
MSA(Microservice Architecture) 와 Monolithic 장단점


DB 질문
본인이 사용한 각 DB의 장단점
설계 할때 어떤걸 가장 고려해서 설계 하는지
trigger, procedure의 차이점
view 테이블의 장점
LEFT JOIN 과 INNER JOIN 차이점
정규화를 하는 이유
트랜잭션이 하는 역할
심화 질문
for 문 안에 await 문으로 처리하는 방식과, Promise.all() 로 처리했을때 차이점
버튼 클릭시 참여 선착순 100명에게만 이벤트를 진행한다고 가정했을때
a. 동작원리를 아는대로 설명
b. 고려 해야 할 상황은 무엇인가?
c. 100번째 버튼을 2명의 사용자가 동시에 눌렀을때 누구에게 우선순위를 부여할 것 인가.


====

프로톤 질문지 - Google Docs

SPA, CSR, SSR의 차이
이벤트 전파 (이벤트 버블링, 이벤트 캡처링)
브라우저 렌더링 원리
(DOM tree구축 -> CSSOM tree 구축 -> js 실행 -> renderTree 구축 -> layout 구축 -> paint 구축)
forEach, Map, Filter 차이점
깊은 복사와 얕은 복사의 차이점
프레임 워크와 라이브러리 차이점
var, let, const 차이점


브라우저 저장소 설명
WebStorage(LocalStorage, SessionStorage), Cookie 차이점
이벤트루프 동작 원리 설명


React
Vanilla vs React의 차이점
useState 구현원리
useEffect, useMemo, useCallback 차이점
useEffect에서 함수를 return 하는 이유
리액트 생명주기 (생성 => 업데이트 => 제거)
React.js 경험이 있을 경우

=============

심화

Q. jwt는 프론트 어디에 탑재되는가

Q. 특정 트래픽 이상으로 튀어버리면 어떻게 해결할 것인가

Q. 기획, 유지보수 등 어느 단계에서라도 데이터와 타입이 바뀌는 순간이 올 수 있다. 어떻게 대처할 것인가
A. 리드 개발자에 의해 데이터들의 룰을 파일로 문서화(swagger도 이용)해서 로컬 빌드과정중에 데이터가 일치하지 않으면 빌드되지 않게끔(의도적으로 build fail 만듦) 유도함. 기타 docx나 xslx를 사용하지 않고 개발자 답게 개발자 문서를 이용하여 git 으로 팀원과 공유하고 전파하여 각자 자기파트가 빌드될 수 있게끔 한다. 이게 restFul에 가까운 방식이다.

Q. k8s 는 작은서비스에선 너무 무겁고 낭비일수 있다. 대안이 있는가.
A. dockerswarm, 경우에 따라선 젠킨스